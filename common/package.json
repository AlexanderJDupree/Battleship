{
  "name": "common",
  "version": "0.1.0",
  "description": "Common library for Battleship game",
  "license": "MIT",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "test": "echo \"Error: no test specified\" && exit 1",
    "testlogic": "node lib/TestGameLogic.js"
  },
  "dependencies": {
    "cross-env": "5.0.5"
  },
  "devDependencies": {
    "typescript": "^4.2.4"
  }
}
